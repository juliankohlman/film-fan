{
	"name": "filmfan",
	"version": "1.0.0",
	"description": "Social network for film fans",
	"main": "server.js",
	"scripts": {
		"client-install": "yarn --prefix client",
		"start": "node server.js",
		"server": "nodemon server.js",
		"client": "yarn --prefix client start",
		"dev": "concurrently 'yarn server' 'yarn client' ",
		"heroku-postbuild": "NPM_CONFIG_PRODUCTION=false yarn --prefix client && yarn run build --prefix client"
	},
	"author": "J.Kohlman",
	"license": "MIT",
	"dependencies": {
		"bcryptjs": "^2.4.3",
		"body-parser": "^1.18.3",
		"concurrently": "^4.0.1",
		"dotenv": "^6.0.0",
		"express": "^4.16.3",
		"gravatar": "^1.6.0",
		"jsonwebtoken": "^8.3.0",
		"mongoose": "^5.2.14",
		"passport": "^0.4.0",
		"passport-jwt": "^4.0.0",
		"validator": "^10.7.1"
	},
	"devDependencies": {
		"nodemon": "^1.18.4"
	}
}
